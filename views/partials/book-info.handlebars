<div class="col-3 text-center pt-5">
    <a href="/books/{{id}}" class="text-decoration-none text-black " style="font-family: 'Courier New', Courier, monospace">
        <div>
            <img src="{{image_url}}" width="300px" height="400px" alt="{{book.title}}">
        </div>
        <div class="pt-1">
            <h3>{{title}}</h3>
        </div>
    </a>
</div>